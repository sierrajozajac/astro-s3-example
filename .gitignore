# Terraform working directory and state. Products of `terraform init` and
# `terraform apply`, never source.
**/.terraform/*
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# Your real values. Commit terraform.tfvars.example instead.
terraform.tfvars
*.auto.tfvars
